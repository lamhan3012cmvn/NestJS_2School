# This config is equivalent to both the '.circleci/extended/orb-free.yml' and the base '.circleci/config.yml'
version: 2.1

orbs:
  node: circleci/node@4.7
jobs:
  deploy:
    docker:
      - image: arvindr226/alpine-ssh
    steps:
      - checkout
      # Please add $SSH_KEY_FINGERPRINT, $DEPLOYED_USER@ and DEPLOYED_SERVER to project's environment
      - add_ssh_keys:
          fingerprints:
            - "24:48:41:d3:9c:fc:14:0e:fb:47:84:9a:c1:5c:18:37"
      - run: ssh -oStrictHostKeyChecking=no root@natha-lms.tk './files/learnCICDnest/deploy.sh'

workflows:
  sample:
    jobs:
      # - node/test:
      #     version: '16.10'
      #     # # If you are using yarn, change the line below from "npm" to "yarn"
      #     pkg-manager: yarn
          #
          # This is the node version to use for the `cimg/node` tag
          # Relevant tags can be found on the CircleCI Developer Hub
          # https://circleci.com/developer/images/image/cimg/node
      # - build-and-push:
      #     requires:
      #       - node/test
          # filters:
          #   branches:
          #     only:
          #       - master
      # uncomment the 3 following lines to enable deploying
      # - deploy:
      #     requires:
      #       - node/test
      - deploy